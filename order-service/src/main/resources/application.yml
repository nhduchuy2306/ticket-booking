spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:9006
  cloud:
    config:
      fail-fast: false
      name: order-service,database,cache,kafka
