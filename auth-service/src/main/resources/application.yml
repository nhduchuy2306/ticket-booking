# ===============================
# Bootstrap Configuration
# All other config is served by Config Server
# ===============================
spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:http://localhost:9006
  cloud:
    config:
      fail-fast: false
      name: auth-service,database,cache,kafka
