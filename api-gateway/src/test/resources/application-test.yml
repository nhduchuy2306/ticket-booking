spring:
  cloud:
    discovery:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: false
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false