spring:
  datasource:
    url: jdbc:mysql://localhost:13306/${service.database.schema}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345
  flyway:
    url: jdbc:mysql://localhost:13306/${service.database.schema}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    user: root
    password: 12345
