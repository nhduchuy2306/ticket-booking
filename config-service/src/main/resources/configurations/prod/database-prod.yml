# ===============================
# Prod Database Configuration
# ===============================
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${service.database.schema}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345
  flyway:
    url: jdbc:mysql://mysql:3306/${service.database.schema}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    user: root
    password: 12345
